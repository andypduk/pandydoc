import Foundation

func runCheckInOutTests(_ client: APIClient) async {
    print("  ⚠️  Check-in/out tests require an existing document with a file - skipped")
    print("  ℹ️  To test manually: POST /api/v1/documents with a file, then use the returned ID")
}
